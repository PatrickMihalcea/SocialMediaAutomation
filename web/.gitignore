node_modules/
.next/
out/
# Every env file except the committed template. Broader than .env/.env.local on
# purpose: a .env.backup or .env.production written during setup holds the same
# secrets and was not covered.
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
/storage/
coverage/
.next-dev/
.server.pid
logs/
.dev.pid
.worker.pid
# Screenshots, downloaded images and reports from the closed-loop e2e run.
e2e-artifacts/
