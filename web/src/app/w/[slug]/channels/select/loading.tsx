import { ChannelsPagePreview, PageTitle } from '@/components/page-previews';

export default function Loading() {
  return (
    <>
      <PageTitle eyebrow="Channels" title="Connect an account" />
      <ChannelsPagePreview />
    </>
  );
}
